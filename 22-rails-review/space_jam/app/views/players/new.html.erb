<%= form_for @player do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :number %>
<%= f.number_field :number%>
<%= f.collection_select :team_id, Team.all, :id, :name %>
<%= f.submit "Come on and Slam!" %>
<% end %>
